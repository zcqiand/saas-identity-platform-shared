# 功能清单（Function Tree）— saas-identity-platform-shared

> **全体系唯一锚点。** 需求、流程、设计、测试都引用这里的 ID。
> 不在这里的 ID 是悬空引用，L5 门会拦。**改功能，先改这份表。**

## 编号规则

| 层级 | 名称 | 格式 | 含义 |
|---|---|---|---|
| 一级 | 功能模块 | `M0x` | 业务域边界，通常对应一级菜单（实际命名见各仓模块总览） |
| 二级 | 功能 | `M0x.F0y` | 一个完整业务步骤 / 独立闭环流程 / 数据管理页面 |
| 三级 | 功能子项 | `M0x.F0y.I0z` | 技术交付单元 / 权限挂载点。对应一个 API 接口、页面组件、图表区块或权限控制点 |

**硬规则**

1. 编号单调递增，永不复用。废弃改状态，不删行。
2. 子项编号必须以父级为前缀。
3. 一个子项 = 一个权限点。权限码即 ID，不另起一套编码。
4. 拆不出子项的功能 → 它其实是子项，往上并。子项超 20 个 → 它其实是模块，往下拆。
5. **改状态，先读 TSP routes + SQL migrations**：状态列必须挂得上 `tsp/routes/*.tsp` 或 `sql/migrations/*.sql` 的具体行号；挂不上就保持「规划」。本仓是契约 BASE，TSP/SQL 是真源，状态不是体感。

**状态**：`规划` | `开发中` | `已上线` | `已废弃`
**子项类型**：`页面` | `标签页` | `查询` | `按钮` | `报表` | `接口`
**交付**：`前端+后端` | `仅前端` | `仅后端` | `后端`（契约 BASE 仓，端点由消费仓 codegen）

## 本仓角色

**契约 BASE 仓**（spec §3）。F+I 级：I 级为全家族定标源；消费仓 ⊆ BASE。

**新结构范围**（2026-09-07 重组）：

- **M00 租户管理**（5 F）— tenant-admin 配置面：`tenant` / `tenant_member` / `tenant_member_role` / `sys_role` / `sys_role_menu` / `tenant_application`
- **M01 用户管理**（4 F）— 当前用户视图 + 自然人关系网：`sys_user` + session / OIDC / token
- **M04 应用管理**（4 F）— 应用 / OAuth client 维度：`oauth_client` / `sys_menu` / `oauth_access_token` / `oauth_refresh_token` / `oauth_code`

**已迁移 / 已废弃段**：M02 角色权限 / M03 SSO 登录 / M05 API Key / M06 审计 / M08 菜单 / M09 菜单授权（详见各段头部说明 + [§0.x 模块重组迁移记录](#0x-模块重组迁移记录2026-09-07)）。

多租户：tenant 跨切是平台默认语义；tenant 真相源在本仓。M97/M98/M99/M96（infra / 契约专属段）不进 BASE，由各消费仓自管。

## 架构全景与三叉戟

> 设计地标：[`docs/design/architecture-panorama.md`](../design/architecture-panorama.md)。
>
> 全景图（实体 × 关系）、三叉戟（应用 / 租户 / 用户）、运行时数据流转链路（AuthN → 选定租户 → 令牌颁发 → AuthZ & Menu）都集中在这份设计地标里。DDL 边界与授权 join 真源见 [`docs/design/target-ddl-permission-isolation.md`](../design/target-ddl-permission-isolation.md)。

## 模块总览

| 模块 ID | 模块名称 | 说明 | 状态 |
|---|---|---|---|
| M00 | 租户管理 | tenant-admin 配置面：5 F（租户维护 / 租户成员 / 租户角色 / 角色权限 / 租户应用） | 开发中 |
| M01 | 用户管理 | 当前用户视图 + 自然人关系网：4 F（用户维护 / 角色成员 / 租户成员 / SSO 登录） | 开发中 |
| M04 | 应用管理 | 应用 / OAuth client 维度：4 F（应用维护 / 应用启用/停用 / 身份认证 / 菜单管理） | 开发中 |

---

## M00 租户管理

> 视角：**tenant-admin 配置面**。op 全部带 `tenantId` 路径或 `/admin/*` 平台 admin 路径，落表覆盖 `tenant` / `tenant_member` / `tenant_member_role` / `sys_role` / `sys_role_menu` / `tenant_application`。

| 功能 ID | 功能名称 | 说明 | 状态 |
|---|---|---|---|
| M00.F01 | 租户维护 | 平台 admin 范围管理租户（与旧 M00.F01 同 op） | 已上线 |
| M00.F02 | 租户成员 | tenant-scoped 成员 CRUD + 邀请/接受/状态（旧 M01.F01 + 部分 M01.F02 + 旧 M01.F03） | 开发中 |
| M00.F03 | 租户角色 | tenant × client 作用域角色 CRUD（旧 M02.F01） | 已上线 |
| M00.F04 | 角色权限 | role↔permission 矩阵 + 角色菜单授权（旧 M02.F02 + 旧 M09.F01/F02） | 开发中 |
| M00.F05 | 租户应用 | `tenant_application` 订阅管理（旧 M04.F04） | 开发中 |

### M00.F01 租户维护

> 端点路径：`/admin/tenants`（要求 platform_admin）

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M00.F01.I01 | 租户列表 | 接口 | 后端 | 平台 admin 分页查看全部租户（含名称/状态过滤；`admin-tenants.tsp:8`） | 已上线 |
| M00.F01.I02 | 创建租户 | 接口 | 后端 | 注册新租户并绑定初始管理员与默认配置（`admin-tenants.tsp:12`） | 已上线 |
| M00.F01.I03 | 租户详情 | 接口 | 后端 | 查看单个租户的完整配置（含状态/到期时间/订阅应用数；`admin-tenants.tsp:17`） | 已上线 |
| M00.F01.I04 | 更新租户 | 接口 | 后端 | 修改租户名称、状态或到期时间等基础字段（`admin-tenants.tsp:22`） | 已上线 |
| M00.F01.I05 | 删除租户 | 接口 | 后端 | 移除租户并级联清理其成员、角色与应用订阅（`admin-tenants.tsp:27`） | 已上线 |

### M00.F02 租户成员

> op 落表：`sys_user` + `tenant_member` + `tenant_member_role`（panorama §2.3）。API 路径用 `userId`，内部按 `(userId, tenant_id)` 解析为 `member_id`；删除只断 membership，不删全局 `sys_user`。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M00.F02.I01 | 成员列表 | 接口 | 后端 | 查看租户内全部成员（分页+角色/状态过滤；`tenant-members.tsp:8`） | 已上线 |
| M00.F02.I02 | 创建成员 | 接口 | 后端 | 在租户下创建成员记录，可一并绑定默认角色（`tenant-members.tsp:17`） | 已上线 |
| M00.F02.I03 | 成员详情 | 接口 | 后端 | 查看成员完整信息（含所属租户、当前角色与状态；`tenant-members.tsp:22`） | 已上线 |
| M00.F02.I04 | 更新成员 | 接口 | 后端 | 修改成员的姓名/邮箱/电话等基础字段（不动角色关系；`tenant-members.tsp:27`） | 已上线 |
| M00.F02.I05 | 删除成员 | 接口 | 后端 | 解除成员与租户的关系（不删除全局用户记录；`tenant-members.tsp:36`） | 已上线 |
| M00.F02.I06 | 邀请成员 | 接口 | 后端 | 生成 7 天有效邀请链接并发送邮件（`tenant-members.tsp:50`） | 已上线 |
| M00.F02.I07 | 接受邀请 | 接口 | 后端 | 凭邀请 token 完成加入；过期返 410（REQ-2026-024 §2.3） | 规划 |
| M00.F02.I08 | 状态切换 | 接口 | 后端 | 启用/停用成员账号（不影响全局用户记录；`tenant-members.tsp:58`） | 已上线 |

### M00.F03 租户角色

> op 落表：`sys_role`（panorama §2.1；作用域 = `tenant_id` × `client_id`）。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M00.F03.I01 | 角色列表 | 接口 | 后端 | 查看租户×client 作用域下全部角色（分页+过滤；`tenant-roles.tsp:8`） | 已上线 |
| M00.F03.I02 | 创建角色 | 接口 | 后端 | 新建一个租户×client 作用域的自定义角色（`tenant-roles.tsp:16`） | 已上线 |
| M00.F03.I03 | 角色详情 | 接口 | 后端 | 查看角色元信息（名称/描述/作用域；`tenant-roles.tsp:21`） | 已上线 |
| M00.F03.I04 | 更新角色 | 接口 | 后端 | 修改角色名称或描述（不动权限与菜单绑定；`tenant-roles.tsp:26`） | 已上线 |
| M00.F03.I05 | 删除角色 | 接口 | 后端 | 移除角色并清理所有成员角色绑定与权限关联（`tenant-roles.tsp:35`） | 已上线 |

### M00.F04 角色权限

> op 落表：`role_permissions` + `sys_role_menu`（panorama §2.1 / §2.3）。配置视角：role 这个身份能做什么（permission + 哪些菜单）。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M00.F04.I01 | 权限矩阵绑定 | 接口 | 后端 | 用权限码 ID 集合全量替换角色绑定的权限（`tenant-roles.tsp:40`） | 已上线 |
| M00.F04.I02 | 角色已授权菜单查询 | 接口 | 后端 | 查看角色当前已分配的菜单 ID 列表（用于 UI 勾选回显；`tenant-role-menus.tsp:8`） | 开发中 |
| M00.F04.I03 | 整批设置角色菜单 | 接口 | 后端 | 用菜单 ID 集合覆盖角色菜单授权（PUT 全量替换；`tenant-role-menus.tsp:14`） | 已上线 |
| M00.F04.I04 | 清空角色菜单 | 接口 | 后端 | 清空角色所有菜单授权（角色登录后不再渲染菜单；`tenant-role-menus.tsp:23`） | 已上线 |

### M00.F05 租户应用

> op 落表：`tenant_application`（panorama §2.2）。订阅状态和到期时间参与授权上下文。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M00.F05.I01 | 列出租户应用 | 接口 | 后端 | 查看租户当前订阅的全部应用（含状态/到期时间；`tenant-applications.tsp:7`） | 开发中 |
| M00.F05.I02 | 订阅应用 | 接口 | 后端 | 为租户启用应用订阅（绑定 clientId 与生效时间；`tenant-applications.tsp:15`） | 开发中 |
| M00.F05.I03 | 更新应用订阅 | 接口 | 后端 | 修改订阅的状态或到期时间（`tenant-applications.tsp:22`） | 开发中 |
| M00.F05.I04 | 移除应用订阅 | 接口 | 后端 | 取消应用订阅（不删除应用本体；`tenant-applications.tsp:31`） | 开发中 |

---

## M01 用户管理

> 视角：**当前用户视图 + 用户作为自然人的关系网**。F01 = 当前用户信息；F02 = 用户被赋予的角色（member→role 视角）；F03 = 当前用户跨租户视图；F04 = 鉴权入口。

| 功能 ID | 功能名称 | 说明 | 状态 |
|---|---|---|---|
| M01.F01 | 用户维护 | 当前用户 whoami（旧 M00.F02.I01） | 开发中 |
| M01.F02 | 角色成员 | 给 member 分配角色（member→role binding；与 M00.F02 字段维护是不同维度） | 已上线 |
| M01.F03 | 租户成员 | 当前用户的跨租户成员关系 + 切换（旧 M00.F02.I02-I03） | 开发中 |
| M01.F04 | SSO 登录 | 密码登录 + 失败锁定 + OIDC + 登出（旧 M03.F01-F03） | 开发中 |

### M01.F01 用户维护

> 当前用户身份视图（自然人）。落表：`sys_user`。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M01.F01.I01 | 当前用户 whoami | 查询 | 后端 | 返回当前会话用户的基础身份信息（id/email/displayName；`me.tsp:8`） | 开发中 |

### M01.F02 角色成员

> op 落表：`tenant_member_role`（panorama §2.3）。**与 M00.F02 字段维护不同维度**——M00.F02 管 member 自身字段/状态/邀请；M01.F02 管 member↔role 关系。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M01.F02.I01 | 分配角色 | 接口 | 后端 | 用角色 ID 集合全量覆盖该成员的当前角色绑定（`tenant-members.tsp:41`） | 已上线 |

### M01.F03 租户成员

> 当前用户视角的跨租户成员关系。落表：`tenant_member`。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M01.F03.I01 | 列出我的租户成员关系 | 查询 | 后端 | 查看当前用户在指定应用下所属的全部租户成员身份（`me.tsp:13`） | 开发中 |
| M01.F03.I02 | 切换当前租户 | 接口 | 后端 | 把当前会话切换到指定租户，更新后续请求的 tenantId 上下文（`me.tsp:18`） | 已上线 |

### M01.F04 SSO 登录

> 鉴权入口：saas session cookie + OIDC + 登出。关联 [REQ-2026-020 §路线 A](../requirements/REQ-2026-020-oauth-session-real-auth.md) + [oauth-architecture.md](oauth-architecture.md)。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M01.F04.I01 | 密码登录 API | 接口 | 后端 | 用邮箱+密码换取 saas session cookie（`auth.tsp:48`） | 已上线 |
| M01.F04.I02 | 失败锁定 | 接口 | 后端 | 连续 5 次密码错误锁定账户 15 分钟，窗口内拒绝登录 | 已上线 |
| M01.F04.I03 | 密码登录 UI | 页面 | 仅前端 | saas-vue / saas-react 的 LoginPage 登录页面 | 规划 |
| M01.F04.I04 | OIDC Code 换取 | 接口 | 后端 | 用 authorization_code 换取 access_token + refresh_token（`auth.tsp:56`） | 已上线 |
| M01.F04.I05 | refresh token | 接口 | 后端 | 用 refresh_token 换取新的 access_token（`auth.tsp:61`） | 已上线 |
| M01.F04.I06 | 登出（本地清理） | 接口 | 后端 | 清理当前浏览器 session cookie（`auth.tsp:51`） | 已上线 |
| M01.F04.I07 | 登出（全局 SSO） | 接口 | 后端 | 预留位；待 ADR 决策 | 规划 |

---

## M04 应用管理

> 视角：**应用 / OAuth client 维度**。F01 管应用 CRUD + 公共元数据；F02 管 status 切换；F03 管 OAuth 身份认证流程；F04 管菜单 + 角色菜单授权 + 当前用户菜单渲染。落表：`oauth_client` / `sys_menu` / `sys_role_menu` / `oauth_access_token` / `oauth_refresh_token` / `oauth_code`（panorama §2.1 / §2.2）。

| 功能 ID | 功能名称 | 说明 | 状态 |
|---|---|---|---|
| M04.F01 | 应用维护 | 应用 CRUD + 公共元数据（旧 M04.F01 + 旧 M04.F05） | 已上线 |
| M04.F02 | 应用启用/停用 | `status` 字段切换（旧 M04.F02） | 已上线 |
| M04.F03 | 身份认证 | OAuth authorize + token + refresh（旧 M04.F03.I07-I09） | 已上线 |
| M04.F04 | 菜单管理 | 菜单 CRUD + 结构 + 当前用户菜单（旧 M08 + 旧 M09 渲染部分） | 开发中 |

### M04.F01 应用维护

> 落表：`oauth_client`（panorama §2.1）。应用是菜单承载 + OAuth client 二合一。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M04.F01.I01 | OAuth 应用列表 | 接口 | 后端 |【平台】平台 admin 列出全部 OAuth 应用（分页+过滤；`admin-apps.tsp:8`） | 已上线 |
| M04.F01.I02 | 创建 OAuth 应用 | 接口 | 后端 |【平台】注册新 OAuth client（含 redirect_uri 白名单与密钥生成；`admin-apps.tsp:12`） | 已上线 |
| M04.F01.I03 | OAuth 应用详情 | 接口 | 后端 |【平台】查看应用完整配置（密钥仅回指纹，不返明文；`admin-apps.tsp:17`） | 已上线 |
| M04.F01.I04 | 更新 OAuth 应用 | 接口 | 后端 |【平台】修改名称/redirect_uri 白名单/允许的 scope 等（`admin-apps.tsp:22`） | 已上线 |
| M04.F01.I05 | 删除 OAuth 应用 | 接口 | 后端 |【平台】移除应用并吊销该 client 名下所有 access/refresh token（`admin-apps.tsp:27`） | 已上线 |
| M04.F01.I06 | 公共 client 元数据 | 接口 | 后端 | 公开端点匿名可读 clientId/name/logo（供登录页应用选择；`apps.tsp:10`） | 已上线 |

### M04.F02 应用启用/停用

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M04.F02.I01 | 启用/停用应用 | 接口 | 后端 |【平台】切换 status 字段；禁用后该 client 的所有 OAuth/token 端点立即拒绝（`admin-apps.tsp:32`） | 已上线 |

### M04.F03 身份认证

> OAuth 2.0 authorization_code + refresh_token；session 强约束（[REQ-2026-020 §路线 A](../requirements/REQ-2026-020-oauth-session-real-auth.md) + [oauth-architecture.md](oauth-architecture.md)）。
> 旧 ID 备注：旧 M04.F03.I07-I09 重编号为 I01-I03（早期实验 I01-I03 与 canonical 同号但不同内容，按时序保留 canonical）。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M04.F03.I01 | 授权码签发 | 接口 | 后端 | 校验 session+redirect_uri+scope 后签发一次性 authorization_code（`oauth.tsp:10`） | 已上线 |
| M04.F03.I02 | 令牌交换 | 接口 | 后端 | 用 authorization_code 换取 access_token + refresh_token（`oauth.tsp:15`） | 已上线 |
| M04.F03.I03 | 令牌刷新 | 接口 | 后端 | 用 refresh_token 换取新的 access_token（可同时轮换 refresh_token；`oauth.tsp:15`） | 已上线 |

### M04.F04 菜单管理

> 落表：`sys_menu` + `sys_role_menu`（panorama §2.1）。子段：菜单 CRUD（I01-I05）+ 菜单结构（I06-I07）+ 当前用户菜单渲染（I08）。
> 角色菜单授权设置端点（`PUT/DELETE /tenants/{t}/roles/{r}/menus`）的 admin 入口在 **M00.F04**（I03-I04）。

| 子项 ID | 名称 | 类型 | 交付 | 说明 | 状态 |
|---|---|---|---|---|---|
| M04.F04.I01 | 菜单列表 | 接口 | 后端 | 查看应用下全部菜单（含层级结构；`admin-app-menus.tsp:8`） | 已上线 |
| M04.F04.I02 | 创建菜单 | 接口 | 后端 | 新建菜单项（含父子关系/icon/path/权限码；`admin-app-menus.tsp:12`） | 已上线 |
| M04.F04.I03 | 菜单详情 | 接口 | 后端 | 查看菜单完整字段（`admin-app-menus.tsp:17`） | 已上线 |
| M04.F04.I04 | 更新菜单 | 接口 | 后端 | 修改菜单名称/path/icon/权限码（不动父子结构；`admin-app-menus.tsp:22`） | 已上线 |
| M04.F04.I05 | 删除菜单 | 接口 | 后端 | 删除菜单并级联清理子菜单与角色菜单授权（`admin-app-menus.tsp:31`） | 已上线 |
| M04.F04.I06 | 同级排序 | 接口 | 后端 | 调整同级菜单的排序顺序（`admin-app-menus.tsp:36`） | 已上线 |
| M04.F04.I07 | 切换父级 | 接口 | 后端 | 把菜单移动到另一父级下（`admin-app-menus.tsp:45`） | 已上线 |
| M04.F04.I08 | 当前用户有效菜单 | 查询 | 后端 | 渲染当前用户在指定应用/租户下有权访问的菜单树（详见 me-menus.md；`me.tsp:23`） | 已上线 |

---

## 已废弃功能子项

所有 `已废弃` / `已迁移` 状态的功能子项（I 级）统一汇总在这里，**按子项 ID 字典序排列**。模块级（F 级及以上）的已废弃 / 已迁移状态保留在各模块章节内说明；本节只收 I 级。

**列入条件**：

- 模块已迁：M02 / M03 / M08 / M09 的全部 I 级子项（已迁移）
- 模块已废：M05 / M06 的全部 I 级子项（已废弃）
- 早期实验段：M04.F03 早期 canonical I01-I03（与新 I01-I03 同号但不同内容，按时序保留早期版本作历史锚点）

| 子项 ID | 名称 | 类型 | 交付 | 说明 / 迁移去向 | 状态 |
|---|---|---|---|---|---|
| M02.F01.I01 | 角色列表（tenant-scoped） | 接口 | 后端 | 旧版：列出租户作用域下的角色 → **M00.F03.I01** | 已迁移 |
| M02.F01.I02 | 创建角色 | 接口 | 后端 | 旧版：在租户下创建角色 → **M00.F03.I02** | 已迁移 |
| M02.F01.I03 | 角色详情 | 接口 | 后端 | 旧版：查看角色元信息 → **M00.F03.I03** | 已迁移 |
| M02.F01.I04 | 更新角色 | 接口 | 后端 | 旧版：修改角色名称或描述 → **M00.F03.I04** | 已迁移 |
| M02.F01.I05 | 删除角色 | 接口 | 后端 | 旧版：删除角色（清理所有绑定） → **M00.F03.I05** | 已迁移 |
| M02.F02.I01 | 权限矩阵 | 接口 | 后端 | 旧版：权限码矩阵全量绑定 → **M00.F04.I01** | 已迁移 |
| M03.F01.I01 | 密码登录 API | 接口 | 后端 | 旧版：邮箱+密码换 session cookie → **M01.F04.I01** | 已迁移 |
| M03.F01.I02 | 失败锁定 | 接口 | 后端 | 旧版：连续失败锁定（5 次/15 min） → **M01.F04.I02** | 已迁移 |
| M03.F01.I03 | 密码登录 UI | 页面 | 仅前端 | 旧版：LoginPage 页面 → **M01.F04.I03** | 已迁移 |
| M03.F02.I03 | OIDC Code 换取 | 接口 | 后端 | 旧版：authorization_code 换 token → **M01.F04.I04** | 已迁移 |
| M03.F02.I04 | refresh token | 接口 | 后端 | 旧版：refresh_token 换新 token → **M01.F04.I05** | 已迁移 |
| M03.F03.I05 | 登出（本地清理） | 接口 | 后端 | 旧版：清理本地 session cookie → **M01.F04.I06** | 已迁移 |
| M03.F03.I06 | 登出（全局 SSO） | 接口 | 后端 | 旧版：全局 SSO 登出（待 ADR 决策） → **M01.F04.I07** | 已迁移 |
| M05.F01.I01 | API Key 列表分页 | 接口 | 后端 | 旧版：分页查询 API Key；目标 DDL 不再包含 `api_keys`，作废 | 已废弃 |
| M05.F01.I02 | API Key 创建 | 接口 | 后端 | 旧版：签发 API Key；目标 DDL 不再包含 `api_keys`，作废 | 已废弃 |
| M05.F01.I03 | API Key 吊销 | 接口 | 后端 | 旧版：吊销 API Key；目标 DDL 不再包含 `api_keys`，作废 | 已废弃 |
| M05.F01.I04 | API Key 轮换 | 接口 | 后端 | 旧版：轮换 API Key；目标 DDL 不再包含 `api_keys`，作废 | 已废弃 |
| M05.F01.I05 | API Key 物理删除 | 接口 | 后端 | 旧版：硬删除 API Key；目标 DDL 不再包含 `api_keys`，作废 | 已废弃 |
| M06.F01.I01 | 列表审计事件 | 接口 | 后端 | 旧版：分页查询审计事件；目标 DDL 不再包含 `audit_events`，作废 | 已废弃 |
| M06.F01.I02 | 按用户查审计事件 | 接口 | 后端 | 旧版：按用户维度过滤审计；目标 DDL 不再包含 `audit_events`，作废 | 已废弃 |
| M06.F01.I03 | 导出审计事件 | 接口 | 后端 | 旧版：导出审计数据；目标 DDL 不再包含 `audit_events`，作废 | 已废弃 |
| M06.F02.I01 | 读取审计留存策略 | 接口 | 后端 | 旧版：读审计留存配置；目标 DDL 不再包含 `audit_retention_policies`，作废 | 已废弃 |
| M06.F02.I02 | 设置审计留存策略 | 接口 | 后端 | 旧版：写审计留存配置；目标 DDL 不再包含 `audit_retention_policies`，作废 | 已废弃 |
| M06.F02.I04 | 留存策略设置 | 接口 | 后端 | 历史重复编号；目标 DDL 全量切换后作废 | 已废弃 |
| M06.F03.I01 | AuditWriter.WriteAsync | 接口 | 后端 | 旧版：审计写入 helper（所有写端点调用）；目标 DDL 不再保留，作废 | 已废弃 |
| M06.F03.I02 | 列表 `?action=` 过滤 | 接口 | 后端 | 旧版：审计列表按 action 过滤；目标 DDL 不再包含审计列表，作废 | 已废弃 |
| M08.F01.I01 | 菜单树 | 接口 | 后端 | 旧版：菜单树形查询 → **M04.F04.I01** | 已迁移 |
| M08.F01.I02 | 创建菜单 | 接口 | 后端 | 旧版：新建菜单 → **M04.F04.I02** | 已迁移 |
| M08.F01.I03 | 菜单详情 | 接口 | 后端 | 旧版：查看菜单 → **M04.F04.I03** | 已迁移 |
| M08.F01.I04 | 更新菜单 | 接口 | 后端 | 旧版：修改菜单字段 → **M04.F04.I04** | 已迁移 |
| M08.F01.I05 | 删除菜单 | 接口 | 后端 | 旧版：删除菜单 → **M04.F04.I05** | 已迁移 |
| M08.F02.I06 | 同级排序 | 接口 | 后端 | 旧版：同级菜单排序 → **M04.F04.I06** | 已迁移 |
| M08.F02.I07 | 切换父级 | 接口 | 后端 | 旧版：菜单父级切换 → **M04.F04.I07** | 已迁移 |
| M09.F01.I01 | 角色已授权菜单 | 查询 | 后端 | 旧版：角色已授权菜单查询 → **M00.F04.I02** | 已迁移 |
| M09.F02.I01 | 整批设置角色菜单 | 接口 | 后端 | 旧版：菜单 ID 集合整批覆盖 → **M00.F04.I03** | 已迁移 |
| M09.F02.I02 | 设置角色菜单（已废弃） | 接口 | 后端 | 旧版：与 I01 重复；保留号不迁移 | 已废弃 |
| M09.F02.I03 | 清空角色菜单 | 接口 | 后端 | 旧版：清空角色所有菜单授权 → **M00.F04.I04** | 已迁移 |
| M09.F03.I01 | me/menus session 校验 | 查询 | 后端 | 旧版：会话校验入口（合并到 my-menu 渲染链路） → **M04.F04.I08** | 已迁移 |
| M09.F03.I02 | 角色授权菜单 ID 查询 | 查询 | 后端 | 旧版：取角色已授权菜单 ID 列表 → **M04.F04.I08** | 已迁移 |
| M09.F03.I03 | 菜单树装配 | 查询 | 后端 | 旧版：装配可见菜单树 → **M04.F04.I08** | 已迁移 |
| M09.F03.I04 | client 分组映射 | 查询 | 后端 | 旧版：client 分组映射 → **M04.F04.I08** | 已迁移 |


### 0.x 模块重组迁移记录（2026-09-07）

> 本节是当前模块结构相对旧版（M00~M09 全集）的迁移依据，下游文档（REQ / design / contract-test）若引用旧 ID，按本表找新位置。

- **M00.F01 租户 CRUD（admin）** → **M00.F01 租户维护**（同 op，名称对齐）
- **M00.F02 当前用户跨租户切换** → 拆分：**M01.F01 用户维护**（I01 whoami）+ **M01.F03 租户成员**（I01 list / I02 switch）
- **M01.F01 用户/成员 CRUD** → **M00.F02 租户成员**（I01-I05，切到租户配置视角）
- **M01.F02 角色分配与状态** → 拆分：**M00.F02 租户成员**（I06-I08：admin 维护 member 字段/状态/邀请）+ **M01.F02 角色成员**（I01：member→role binding）
- **M01.F03 邀请接受** → **M00.F02 租户成员**（I07 接受邀请，归 tenant-admin 闭环）
- **M02.F01 角色 CRUD** → **M00.F03 租户角色**（切到租户配置视角）
- **M02.F02 权限矩阵绑定** → **M00.F04 角色权限**（I01）
- **M03.F01-F03 SSO + OIDC + logout** → **M01.F04 SSO 登录**（I01-I07，归用户管理；auth 是用户进入的入口）
- **M04.F01 应用 CRUD** → **M04.F01 应用维护**（I01-I05，同 op）
- **M04.F02 启用/停用** → **M04.F02 应用启用/停用**（I01，同 op）
- **M04.F03 OAuth 授权码+令牌** → **M04.F03 身份认证**（I01-I03，名称对齐"身份认证"）
- **M04.F04 租户应用订阅** → **M00.F05 租户应用**（I01-I04，切到租户配置视角）
- **M04.F05 应用公开元数据** → **M04.F01 应用维护**（I06，合并到应用维护）
- **M08.F01-F02 菜单 CRUD/结构** → **M04.F04 菜单管理**（I01-I07，切到应用配置视角）
- **M09.F01-F03 角色菜单授权 + 我的菜单** → 拆分：**M00.F04 角色权限**（I02-I04 角色菜单 admin 授权）+ **M04.F04 菜单管理**（I08 当前用户菜单渲染）
- **M05 / M06（已废弃）** → 不变；已废止，目标 DDL 不再保留

> 旧 ID 全部状态保持「已迁移（迁至新 ID）」，不删除行；下游合同测试 / 消费仓按本表换 ID。
> I-level 编号：新 F 容器内重新编号，旧 I 编号不再保留。contract-test 与消费仓需在升级时同步。
> 已迁移 / 已废弃 F 段下的旧 I 子项已统一移至 [§ 已废弃功能子项](#已废弃功能子项)。

---

## 维护约定

- 谁改功能，谁改表，同一个 commit。
- `规划` → `开发中`：必须先有需求文档引用它。
- `开发中` → `已上线`：L5 会警告它缺设计映射与测试引用。警告不阻断，由人裁量。
- **已废弃 / 已迁移 I 级子项**：从原 F 段移到「已废弃功能子项」段，按子项 ID 字典序排列；F 级保留在各模块章节内作历史锚点。
- 旧 ID 状态变更走 `[§0.x 模块重组迁移记录](#0x-模块重组迁移记录2026-09-07)`，不直接删行。