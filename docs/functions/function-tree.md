# 功能清单（Function Tree）— saas-identity-platform-shared

> **全体系唯一锚点。** 需求、流程、设计、测试都引用这里的 ID。
> 不在这里的 ID 是悬空引用，L5 门会拦。**改功能，先改这份表。**

## 编号规则

| 层级 | 名称     | 格式            | 含义                                                                         |
| ---- | -------- | --------------- | ---------------------------------------------------------------------------- |
| 一级 | 功能模块 | `M0x`         | 业务域边界，通常对应一级菜单（实际命名见各仓模块总览）                       |
| 二级 | 功能     | `M0x.F0y`     | 一个完整业务步骤 / 独立闭环流程 / 数据管理页面                               |
| 三级 | 功能子项 | `M0x.F0y.I0z` | 技术交付单元 / 权限挂载点。对应一个 API 接口、页面组件、图表区块或权限控制点 |

**硬规则**

1. 编号单调递增，永不复用。废弃改状态，不删行。
2. 子项编号必须以父级为前缀。
3. 一个子项 = 一个权限点。权限码即 ID，不另起一套编码。
4. 拆不出子项的功能 → 它其实是子项，往上并。子项超 20 个 → 它其实是模块，往下拆。
5. **改状态，先读 TSP routes + SQL migrations**：状态列必须挂得上 `tsp/routes/*.tsp` 或 `sql/migrations/*.sql` 的具体行号；挂不上就保持「规划」。本仓是契约 BASE，TSP/SQL 是真源，状态不是体感。

**状态**：`规划` | `开发中` | `已上线` | `已废弃`

**子项类型**（仓视角，6 值）：`页面` | `标签页` | `查询` | `按钮` | `报表` | `接口`
各仓自标——react/vue 标页面、springboot/aspnetcore 标接口、nextjs 端点标接口 / 派生标查询。继续驱动 `_alignment.py` FACING_TYPES 的 UI 入口锚点检查。

**交付**（全局视角，3 值）：`前端+后端` | `仅前端` | `仅后端`
**同一 ID 全家族同标**——BASE 定标，5 仓镜像照抄（[ADR-0024](../../../docs/adr/0024-function-tree-base-consumer-field-alignment.md) §Decision「消费仓照抄 BASE」）。判定标准（[spec §2](https://example.com/spec) 锁定）：

| 交付值        | 判定                                                  | 例子                                           |
| ------------- | ----------------------------------------------------- | ---------------------------------------------- |
| `仅前端`    | 纯 UI 交互 / 组件 / 路由守卫 / 客户端状态，不涉及 API | 路由守卫、BackendBadge 展示                    |
| `前端+后端` | UI + API 闭环（同一 I 全家族同标）                    | 租户列表 = 页面 +`GET /api/v1/admin/tenants` |
| `仅后端`    | 无 UI。纯端点 / infra / schema 脚本                   | `POST /api/v1/auth/refresh`、replay V*.sql   |

**上线口径**（[spec §4](https://example.com/spec) 锁定）：

- BASE 状态 = 功能全局交付进度；消费仓状态 = 本仓那半边交付了没有。**仓只为自己交付的那一半负责**。
- BASE 已上线判据：`仅后端`=4 后端+msw 全已上线；`仅前端`=3 前端全已上线；`前端+后端`=前后两半都齐。
- 跨仓状态合法性：BASE 已上线 → 所有适用仓必须已上线（否则 gap）；单仓已上线 + BASE 开发中 → 合法；**前端仓上线「仅后端」项 / 后端仓上线「仅前端」项 → 状态矛盾 = 数据错误**。

> 设计地标：[`docs/design/iid-binding-rules.md`](../design/iid-binding-rules.md)（I ID ↔ SSOT 端点 / 数据表 绑定规则）+ [`docs/conventions/sync-architecture.md`](../../../../docs/conventions/sync-architecture.md)（跨仓同步架构）。

## 本仓角色

**契约 BASE 仓**（spec §3）。F+I 级：I 级为全家族定标源；消费仓 ⊆ BASE。

**新结构范围**（2026-09-07 重组）：

- **M00 租户管理**（5 F）— tenant-admin 配置面：`tenant` / `tenant_member` / `tenant_member_role` / `sys_role` / `sys_role_menu` / `tenant_application`
- **M01 用户管理**（4 F）— 当前用户视图 + 自然人关系网：`sys_user` + session / OIDC / token
- **M04 应用管理**（4 F）— 应用 / OAuth client 维度：`oauth_client` / `sys_menu` / `oauth_access_token` / `oauth_refresh_token` / `oauth_code`

**已废弃段**：M02 角色权限 / M03 SSO 登录 / M05 API Key / M06 审计 / M08 菜单 / M09 菜单授权（详见各段头部说明 + [§0.x 模块重组迁移记录](#0x-模块重组迁移记录2026-09-07)）。

多租户：tenant 跨切是平台默认语义；tenant 真相源在本仓。M97/M98/M99/M96（infra / 契约专属段）不进 BASE，由各消费仓自管。

## 架构全景与三叉戟

> 设计地标：[`docs/design/architecture-panorama.md`](../design/architecture-panorama.md)。
>
> 全景图（实体 × 关系）、三叉戟（应用 / 租户 / 用户）、运行时数据流转链路（AuthN → 选定租户 → 令牌颁发 → AuthZ & Menu）都集中在这份设计地标里。DDL 边界与授权 join 真源见 [`docs/design/target-ddl-permission-isolation.md`](../design/target-ddl-permission-isolation.md)。

## 模块总览

| 模块 ID | 模块名称 | 说明                                                                             | 状态   |
| ------- | -------- | -------------------------------------------------------------------------------- | ------ |
| M00     | 租户管理 | tenant-admin 配置面：5 F（租户维护 / 租户成员 / 租户角色 / 角色权限 / 租户应用） | 开发中 |
| M01     | 用户管理 | 当前用户视图 + 自然人关系网：4 F（用户维护 / 角色成员 / 租户成员 / SSO 登录）    | 开发中 |
| M04     | 应用管理 | 应用 / OAuth client 维度：4 F（应用维护 / 应用启用/停用 / 身份认证 / 菜单管理）  | 开发中 |

---

## M00 租户管理

> 视角：**tenant-admin 配置面**。op 全部带 `tenantId` 路径或 `/admin/*` 平台 admin 路径，落表覆盖 `tenant` / `tenant_member` / `tenant_member_role` / `sys_role` / `sys_role_menu` / `tenant_application`。

| 功能 ID | 功能名称 | 说明                                                                               | 状态   |
| ------- | -------- | ---------------------------------------------------------------------------------- | ------ |
| M00.F01 | 租户维护 | 平台 admin 范围管理租户（与旧 M00.F01 同 op）                                      | 已上线 |
| M00.F02 | 租户成员 | tenant-scoped 成员 CRUD + 邀请/接受/状态（旧 M01.F01 + 部分 M01.F02 + 旧 M01.F03） | 开发中 |
| M00.F03 | 租户角色 | tenant × client 作用域角色 CRUD（旧 M02.F01）                                     | 已上线 |
| M00.F04 | 角色权限 | role↔permission 矩阵 + 角色菜单授权（旧 M02.F02 + 旧 M09.F01/F02）                | 开发中 |
| M00.F05 | 租户应用 | `tenant_application` 订阅管理（旧 M04.F04）                                      | 已上线 |

### M00.F01 租户维护

> 端点路径：`/admin/tenants`（要求 platform_admin）

| 子项 ID     | 名称     | 类型 | 交付      | 说明                                                                           | 状态   |
| ----------- | -------- | ---- | --------- | ------------------------------------------------------------------------------ | ------ |
| M00.F01.I01 | 租户列表 | 接口 | 前端+后端 | 平台 admin 分页查看全部租户（含名称/状态过滤；`admin-tenants.tsp:8`）        | 已上线 |
| M00.F01.I02 | 创建租户 | 接口 | 前端+后端 | 注册新租户并绑定初始管理员与默认配置（`admin-tenants.tsp:12`）               | 已上线 |
| M00.F01.I03 | 租户详情 | 接口 | 前端+后端 | 查看单个租户的完整配置（含状态/到期时间/订阅应用数；`admin-tenants.tsp:16`） | 已上线 |
| M00.F01.I04 | 更新租户 | 接口 | 前端+后端 | 修改租户名称、状态或到期时间等基础字段（`admin-tenants.tsp:21`）             | 已上线 |
| M00.F01.I05 | 删除租户 | 接口 | 前端+后端 | 移除租户并级联清理其成员、角色与应用订阅（`admin-tenants.tsp:26`）           | 已上线 |

### M00.F02 租户成员

> op 落表：`sys_user` + `tenant_member` + `tenant_member_role`（panorama §2.3）。API 路径用 `userId`，内部按 `(userId, tenant_id)` 解析为 `member_id`；删除只断 membership，不删全局 `sys_user`。

| 子项 ID     | 名称     | 类型 | 交付      | 说明                                                                        | 状态   |
| ----------- | -------- | ---- | --------- | --------------------------------------------------------------------------- | ------ |
| M00.F02.I01 | 成员列表 | 接口 | 前端+后端 | 查看租户内全部成员（分页+角色/状态过滤；`tenant-members.tsp:11`）            | 已上线 |
| M00.F02.I02 | 创建成员 | 接口 | 前端+后端 | 在租户下创建成员记录，可一并绑定默认角色（`tenant-members.tsp:20`）         | 已上线 |
| M00.F02.I03 | 成员详情 | 接口 | 前端+后端 | 查看成员完整信息（含所属租户、当前角色与状态；`tenant-members.tsp:24`）     | 已上线 |
| M00.F02.I04 | 更新成员 | 接口 | 前端+后端 | 修改成员的姓名/邮箱/电话等基础字段（不动角色关系；`tenant-members.tsp:29`） | 已上线 |
| M00.F02.I05 | 删除成员 | 接口 | 前端+后端 | 解除成员与租户的关系（不删除全局用户记录；`tenant-members.tsp:38`）         | 已上线 |
| M00.F02.I06 | 邀请成员 | 接口 | 前端+后端 | 生成 7 天有效邀请链接并发送邮件（`tenant-members.tsp:53`）                  | 已上线 |
| M00.F02.I07 | 接受邀请 | 接口 | 前端+后端 | 凭邀请 token 完成加入；过期返 410（REQ-2026-024 §2.3）                      | 规划   |
| M00.F02.I08 | 状态切换 | 接口 | 前端+后端 | 启用/停用成员账号（不影响全局用户记录；`tenant-members.tsp:61`）            | 已上线 |

### M00.F03 租户角色

> op 落表：`sys_role`（panorama §2.1；作用域 = `tenant_id` × `client_id`）。

| 子项 ID     | 名称     | 类型 | 交付      | 说明                                                                   | 状态   |
| ----------- | -------- | ---- | --------- | ---------------------------------------------------------------------- | ------ |
| M00.F03.I01 | 角色列表 | 接口 | 前端+后端 | 查看租户×client 作用域下全部角色（分页+过滤；`tenant-roles.tsp:8`） | 已上线 |
| M00.F03.I02 | 创建角色 | 接口 | 前端+后端 | 新建一个租户×client 作用域的自定义角色（`tenant-roles.tsp:17`）     | 已上线 |
| M00.F03.I03 | 角色详情 | 接口 | 前端+后端 | 查看角色元信息（名称/描述/作用域；`tenant-roles.tsp:21`）            | 已上线 |
| M00.F03.I04 | 更新角色 | 接口 | 前端+后端 | 修改角色名称或描述（不动权限与菜单绑定；`tenant-roles.tsp:26`）      | 已上线 |
| M00.F03.I05 | 删除角色 | 接口 | 前端+后端 | 移除角色并清理所有成员角色绑定与权限关联（`tenant-roles.tsp:35`）    | 已上线 |

### M00.F04 角色权限

> op 落表：`role_permissions` + `sys_role_menu`（panorama §2.1 / §2.3）。配置视角：role 这个身份能做什么（permission + 哪些菜单）。

| 子项 ID     | 名称               | 类型 | 交付      | 说明                                                                              | 状态   |
| ----------- | ------------------ | ---- | --------- | --------------------------------------------------------------------------------- | ------ |
| M00.F04.I01 | ~~权限矩阵绑定~~   | 接口 | 前端+后端 | ~~用权限码 ID 集合全量替换角色绑定的权限~~（v0.3.0 重组后 4 后端 + msw 全部废弃 role_permissions 表与端点；详见 deprecated-items.md §1） | 已废弃 |
| M00.F04.I02 | 角色已授权菜单查询 | 接口 | 前端+后端 | 查看角色当前已分配的菜单 ID 列表（用于 UI 勾选回显；`tenant-role-menus.tsp:9`） | 开发中 |
| M00.F04.I03 | 整批设置角色菜单   | 接口 | 前端+后端 | 用菜单 ID 集合覆盖角色菜单授权（PUT 全量替换；`tenant-role-menus.tsp:17`）      | 已上线 |
| M00.F04.I04 | 清空角色菜单       | 接口 | 前端+后端 | 清空角色所有菜单授权（角色登录后不再渲染菜单；`tenant-role-menus.tsp:26`）      | 已上线 |

### M00.F05 租户应用

> op 落表：`tenant_application`（panorama §2.2）。订阅状态和到期时间参与授权上下文。

| 子项 ID     | 名称         | 类型 | 交付      | 说明                                                                           | 状态   |
| ----------- | ------------ | ---- | --------- | ------------------------------------------------------------------------------ | ------ |
| M00.F05.I01 | 列出租户应用 | 接口 | 前端+后端 | 查看租户当前订阅的全部应用（含状态/到期时间；`tenant-applications.tsp:8`）   | 已上线 |
| M00.F05.I02 | 订阅应用     | 接口 | 前端+后端 | 为租户启用应用订阅（绑定 clientId 与生效时间；`tenant-applications.tsp:16`） | 已上线 |
| M00.F05.I03 | 更新应用订阅 | 接口 | 前端+后端 | 修改订阅的状态或到期时间（`tenant-applications.tsp:23`）                     | 已上线 |
| M00.F05.I04 | 移除应用订阅 | 接口 | 前端+后端 | 取消应用订阅（不删除应用本体；`tenant-applications.tsp:32`）                 | 已上线 |

---

## M01 用户管理

> 视角：**当前用户视图 + 用户作为自然人的关系网**。F01 = 当前用户信息；F02 = 用户被赋予的角色（member→role 视角）；F03 = 当前用户跨租户视图；F04 = 鉴权入口。

| 功能 ID | 功能名称 | 说明                                                                      | 状态   |
| ------- | -------- | ------------------------------------------------------------------------- | ------ |
| M01.F01 | 用户维护 | 当前用户 whoami（旧 M00.F02.I01）                                         | 开发中 |
| M01.F02 | 角色成员 | 给 member 分配角色（member→role binding；与 M00.F02 字段维护是不同维度） | 已上线 |
| M01.F03 | 租户成员 | 当前用户的跨租户成员关系 + 切换（旧 M00.F02.I02-I03）                     | 开发中 |
| M01.F04 | SSO 登录 | 密码登录 + 失败锁定 + OIDC + 登出（旧 M03.F01-F03）                       | 开发中 |

### M01.F01 用户维护

> 当前用户身份视图（自然人）。落表：`sys_user`。

| 子项 ID     | 名称            | 类型 | 交付      | 说明                                                                 | 状态   |
| ----------- | --------------- | ---- | --------- | -------------------------------------------------------------------- | ------ |
| M01.F01.I01 | 当前用户 whoami | 查询 | 前端+后端 | 返回当前会话用户的基础身份信息（id/email/displayName；`me.tsp:8`） | 开发中 |

### M01.F02 角色成员

> op 落表：`tenant_member_role`（panorama §2.3）。**与 M00.F02 字段维护不同维度**——M00.F02 管 member 自身字段/状态/邀请；M01.F02 管 member↔role 关系。

| 子项 ID     | 名称     | 类型 | 交付      | 说明                                                                  | 状态   |
| ----------- | -------- | ---- | --------- | --------------------------------------------------------------------- | ------ |
| M01.F02.I01 | 分配角色 | 接口 | 前端+后端 | 用角色 ID 集合全量覆盖该成员的当前角色绑定（`tenant-members.tsp:43`） | 已上线 |

### M01.F03 租户成员

> 当前用户视角的跨租户成员关系。落表：`tenant_member`。

| 子项 ID     | 名称                 | 类型 | 交付      | 说明                                                                      | 状态   |
| ----------- | -------------------- | ---- | --------- | ------------------------------------------------------------------------- | ------ |
| M01.F03.I01 | 列出我的租户成员关系 | 查询 | 前端+后端 | 查看当前用户在指定应用下所属的全部租户成员身份（`me.tsp:13`）           | 开发中 |
| M01.F03.I02 | 切换当前租户         | 接口 | 前端+后端 | 把当前会话切换到指定租户，更新后续请求的 tenantId 上下文（`me.tsp:18`） | 已上线 |

### M01.F04 SSO 登录

> 鉴权入口：saas session cookie + OIDC + 登出。关联 [REQ-2026-020 §路线 A](../requirements/REQ-2026-020-oauth-session-real-auth.md) + [oauth-architecture.md](oauth-architecture.md)。

| 子项 ID     | 名称             | 类型 | 交付      | 说明                                                                       | 状态   |
| ----------- | ---------------- | ---- | --------- | -------------------------------------------------------------------------- | ------ |
| M01.F04.I01 | 密码登录 | 接口 | 前端+后端 | `/auth/login` 邮箱+密码换取 saas session cookie；连续 5 次密码错返回 HTTP 423 Locked；前端 LoginPage（saas-vue / saas-react）走本端点（合并 I02；I03 于 2026-09-19 人裁恢复独立项；`sessions.tsp:55-59`）  | 已上线  |
| M01.F04.I02 | ~~失败锁定~~                       | 接口 | 仅后端 | ~~连续 5 次密码错误锁定账户 15 分钟，窗口内拒绝登录~~（合并到 I01）        | 已废弃 |
| M01.F04.I03 | 密码登录 UI | 页面 | 仅前端 | saas-vue / saas-react / saas-nextjs 的 LoginPage 登录页面（2026-09-19 人裁撤销废弃恢复正式项——前端 3 仓 UI 实存在用，页面项以 UI 实存为准） | 已上线 |
| M01.F04.I04 | ~~OIDC Code 换取~~                  | 接口 | 仅后端 | ~~用 authorization_code 换取 access_token + refresh_token~~（与 /oauth/token authorization_code grant 重复；废弃）  | 已废弃 |
| M01.F04.I05 | ~~refresh token~~                   | 接口 | 仅后端 | ~~用 refresh_token 换取新的 access_token~~（与 /oauth/token refresh_token grant 重复；废弃）                | 已废弃 |
| M01.F04.I06 | 登出（本地清理 + 全局 SSO）          | 接口 | 前端+后端 | `/auth/logout` 清理当前浏览器 session cookie + 全局 SSO logout（合并 I07；`sessions.tsp:63`）                | 已上线  |
| M01.F04.I07 | ~~登出（全局 SSO）~~                | 接口 | 前端+后端 | ~~预留位；待 ADR 决策~~（合并到 I06）                                                          | 已废弃 |

---

## M04 应用管理

> 视角：**应用 / OAuth client 维度**。F01 管应用 CRUD + 公共元数据；F02 管 status 切换；F03 管 OAuth 身份认证流程；F04 管菜单 + 角色菜单授权 + 当前用户菜单渲染。落表：`oauth_client` / `sys_menu` / `sys_role_menu` / `oauth_access_token` / `oauth_refresh_token` / `oauth_code`（panorama §2.1 / §2.2）。

| 功能 ID | 功能名称      | 说明                                                        | 状态   |
| ------- | ------------- | ----------------------------------------------------------- | ------ |
| M04.F01 | 应用维护      | 应用 CRUD + 公共元数据（旧 M04.F01 + 旧 M04.F05）           | 已上线 |
| M04.F02 | 应用启用/停用 | `status` 字段切换（旧 M04.F02）                           | 已上线 |
| M04.F03 | 身份认证      | OAuth authorize + token + refresh（旧 M04.F03.I07-I09）     | 已上线 |
| M04.F04 | 菜单管理      | 菜单 CRUD + 结构 + 当前用户菜单（旧 M08 + 旧 M09 渲染部分） | 开发中 |

### M04.F01 应用维护

> 落表：`oauth_client`（panorama §2.1）。应用是菜单承载 + OAuth client 二合一。

| 子项 ID     | 名称               | 类型 | 交付      | 说明                                                                                     | 状态   |
| ----------- | ------------------ | ---- | --------- | ---------------------------------------------------------------------------------------- | ------ |
| M04.F01.I01 | OAuth 应用列表     | 接口 | 前端+后端 | 【平台】平台 admin 列出全部 OAuth 应用（分页+过滤；`admin-clients.tsp:8`）               | 已上线 |
| M04.F01.I02 | 创建 OAuth 应用    | 接口 | 前端+后端 | 【平台】注册新 OAuth client（含 redirect_uri 白名单与密钥生成；`admin-clients.tsp:12`）  | 已上线 |
| M04.F01.I03 | OAuth 应用详情     | 接口 | 前端+后端 | 【平台】查看应用完整配置（密钥仅回指纹，不返明文；`admin-clients.tsp:16`）               | 已上线 |
| M04.F01.I04 | 更新 OAuth 应用    | 接口 | 前端+后端 | 【平台】修改名称/redirect_uri 白名单/允许的 scope 等（`admin-clients.tsp:21`）           | 已上线 |
| M04.F01.I05 | 删除 OAuth 应用    | 接口 | 前端+后端 | 【平台】移除应用并吊销该 client 名下所有 access/refresh token（`admin-clients.tsp:26`）  | 已上线 |
| M04.F01.I06 | 公共 client 元数据 | 接口 | 前端+后端 | 公开端点匿名可读 clientId/name/logo（供登录页应用选择；`clients.tsp:10`）                | 已上线 |

### M04.F02 应用启用/停用

| 子项 ID     | 名称          | 类型 | 交付      | 说明                                                                                                 | 状态   |
| ----------- | ------------- | ---- | --------- | ---------------------------------------------------------------------------------------------------- | ------ |
| M04.F02.I01 | 启用/停用应用 | 接口 | 前端+后端 | 【平台】切换 status 字段；禁用后该 client 的所有 OAuth/token 端点立即拒绝（`admin-clients.tsp:32`）  | 已上线 |

### M04.F03 身份认证

> OAuth 2.0 authorization_code + refresh_token；session 强约束（[REQ-2026-020 §路线 A](../requirements/REQ-2026-020-oauth-session-real-auth.md) + [oauth-architecture.md](oauth-architecture.md)）。
> 旧 ID 备注：旧 M04.F03.I07-I09 重编号为 I01-I03（早期实验 I01-I03 与 canonical 同号但不同内容，按时序保留 canonical）。

| 子项 ID     | 名称       | 类型 | 交付      | 说明                                                                                 | 状态   |
| ----------- | ---------- | ---- | --------- | ------------------------------------------------------------------------------------ | ------ |
| M04.F03.I01 | 授权码签发 | 接口 | 前端+后端 | 校验 session+redirect_uri+scope 后签发一次性 authorization_code（`oauth.tsp:10`）  | 已上线 |
| M04.F03.I02 | OIDC token 端点（双 grant） | 接口 | 前端+后端 | `/oauth/token` 接收 authorization_code / refresh_token 双 grant，返回 access_token（可同时轮换 refresh_token；`oauth.tsp:15`；合并 I03） | 已上线 |

### M04.F04 菜单管理

> 落表：`sys_menu` + `sys_role_menu`（panorama §2.1）。子段：菜单 CRUD（I01-I05）+ 菜单结构（I06-I07）+ 当前用户菜单渲染（I08）。
> 角色菜单授权设置端点（`PUT/DELETE /tenants/{t}/roles/{r}/menus`）的 admin 入口在 **M00.F04**（I03-I04）。

| 子项 ID     | 名称             | 类型 | 交付      | 说明                                                                             | 状态   |
| ----------- | ---------------- | ---- | --------- | -------------------------------------------------------------------------------- | ------ |
| M04.F04.I01 | 菜单列表         | 接口 | 前端+后端 | 查看应用下全部菜单（含层级结构；`client-menus.tsp:8`）                           | 已上线 |
| M04.F04.I02 | 创建菜单         | 接口 | 前端+后端 | 新建菜单项（含父子关系/icon/path/权限码；`client-menus.tsp:12`）                 | 已上线 |
| M04.F04.I03 | 菜单详情         | 接口 | 前端+后端 | 查看菜单完整字段（`client-menus.tsp:16`）                                        | 已上线 |
| M04.F04.I04 | 更新菜单         | 接口 | 前端+后端 | 修改菜单名称/path/icon/权限码（不动父子结构；`client-menus.tsp:21`）             | 已上线 |
| M04.F04.I05 | 删除菜单         | 接口 | 前端+后端 | 删除菜单并级联清理子菜单与角色菜单授权（`client-menus.tsp:30`）                  | 已上线 |
| M04.F04.I06 | 同级排序         | 接口 | 前端+后端 | 调整同级菜单的排序顺序（`client-menus.tsp:35`）                                  | 已上线 |
| M04.F04.I07 | 切换父级         | 接口 | 前端+后端 | 把菜单移动到另一父级下（`client-menus.tsp:44`）                                  | 已上线 |
| M04.F04.I08 | 当前用户有效菜单 | 查询 | 前端+后端 | 渲染当前用户在指定应用/租户下有权访问的菜单树（详见 me-menus.md；`me.tsp:26`）   | 已上线 |

---

## 已废弃功能子项（历史档案）

详见 [deprecated-items.md](./deprecated-items.md)（历史档案文件）—— 包含：

- § 1 已废弃功能子项表（M02 / M03 / M05 / M06 / M08 / M09 共 40 条 I ID + 迁移去向）
- § 2 模块重组迁移记录（2026-09-07，旧 → 新 ID 映射）
- § 3 维护约定（已废弃段专用）
- § 4 跨文档引用

---

## 维护约定

- 谁改功能，谁改表，同一个 commit。
- `规划` → `开发中`：必须先有需求文档引用它。
- `开发中` → `已上线`：L5 会警告它缺设计映射与测试引用。警告不阻断，由人裁量。
- **已废弃 I 级子项**：从原 F 段移到「已废弃功能子项」段，按子项 ID 字典序排列；F 级保留在各模块章节内作历史锚点。
- 旧 ID 状态变更走 `[§0.x 模块重组迁移记录](#0x-模块重组迁移记录2026-09-07)`，不直接删行。